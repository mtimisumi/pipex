< file1 grep "text" | wc -l > file2

< file1 = reads from file1
grep "text" = searches for lines containing "text"
| wc -1 = pipes the filtered lines to sc -1, which count the number of matching lines
> file2 = saves the final count into file2

pipe(|) = to connect the output of one command directly into the input of another